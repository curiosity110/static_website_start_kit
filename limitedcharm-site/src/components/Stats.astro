---
interface Props {
  stats: { label: string; value: string }[];
}
const { stats } = Astro.props;
---
<section>
  <ul class="flex gap-4">
    {stats.map((s) => (
      <li>
        <p class="text-2xl">{s.value}</p>
        <p>{s.label}</p>
      </li>
    ))}
  </ul>
</section>